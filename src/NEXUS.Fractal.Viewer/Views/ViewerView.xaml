<UserControl x:Class="NEXUS.Fractal.Viewer.Views.ViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:NEXUS.Fractal.Viewer.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:wpf="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             xmlns:project="clr-namespace:NEXUS.Fractal.Core.ViewModels.Project;assembly=NEXUS.Fractal.Core"
             xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDark.xaml" />
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <DockPanel>
        <ToolBar DockPanel.Dock="Top">
            <Button>
                <wpf:MaterialIcon Kind="TableColumn"/>
            </Button>
        </ToolBar>
        <hc:TabControl ShowCloseButton="True" ShowContextMenu="False"
                       ItemsSource="{Binding ProjectService.SelectedEntities}"
                       SelectedItem="{Binding SelectedProjectEntity}">
        <b:Interaction.Triggers>
            <b:EventTrigger EventName="SelectionChanged">
                <b:InvokeCommandAction Command="{Binding SelectionChangedCommand}"/>
            </b:EventTrigger>
        </b:Interaction.Triggers>
        <TabControl.ItemTemplate>
            <DataTemplate DataType="{x:Type project:ProjectEntityViewModel}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="4" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <wpf:MaterialIcon Grid.Column="0" Kind="MountainOutline" />
                    <TextBlock VerticalAlignment="Center" Grid.Column="2" Text="{Binding  Name}" />
                </Grid>
            </DataTemplate>
        </TabControl.ItemTemplate>
        <TabControl.ContentTemplate>
            <DataTemplate DataType="{x:Type project:ProjectEntityViewModel}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TabControl Grid.Column="0" TabStripPlacement="Bottom">
                        <TabItem>
                            <TabItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <wpf:MaterialIcon Kind="Image" />
                                    <TextBlock VerticalAlignment="Center" Text="Изображение" />
                                </StackPanel>
                            </TabItem.Header>
                            <Grid>
                                <Image Source="{Binding Image}"/>
                                <!-- <hc:ImageViewer ImageSource="{Binding Image}"/> -->
                            </Grid>
                        </TabItem>
                        <TabItem>
                            <TabItem.Header>
                                <StackPanel Orientation="Horizontal">
                                    <wpf:MaterialIcon Kind="CubeOutline" />
                                    <TextBlock VerticalAlignment="Center" Text="Модель" />
                                </StackPanel>
                            </TabItem.Header>
                            <wpf:MaterialIcon Kind="CubeOutline" />
                        </TabItem>
                    </TabControl>
                    <hc:RangeSlider Grid.Column="1" Orientation="Vertical"/>
                </Grid>
            </DataTemplate>
        </TabControl.ContentTemplate>
    </hc:TabControl>
    </DockPanel>
    
</UserControl>