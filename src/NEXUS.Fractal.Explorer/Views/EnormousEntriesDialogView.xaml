<UserControl x:Class="NEXUS.Fractal.Explorer.Views.EnormousEntriesDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             xmlns:wpf="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignWidth="300" d:DesignHeight="200"
             mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDark.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <prism:Dialog.WindowStyle>
        <Style TargetType="hc:Window">
            <Setter Property="Background" Value="{DynamicResource RegionBrush}" />
        </Style>
    </prism:Dialog.WindowStyle>

    <!-- Dialog content with HandyControl controls -->
    <Grid Background="{DynamicResource RegionBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="8"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="8"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1" Grid.Row="1" Grid.ColumnSpan="3" Orientation="Horizontal">
            <wpf:MaterialIcon Foreground="{StaticResource TextIconBrush}" Kind="Warning"/>
            <TextBlock VerticalAlignment="Center" Margin="8 0" Text="{Binding Title}"
                       Style="{StaticResource TextBlockTitleBold}"/>
        </StackPanel>
        <TextBlock Grid.Column="1" Grid.Row="3" Grid.ColumnSpan="3" Text="{Binding Message}"
                    Style="{StaticResource TextBlockDefaultBold}"/>
        <Button HorizontalAlignment="Stretch" Grid.Column="1" Grid.Row="5" Command="{Binding CloseCommand}">
            <StackPanel Orientation="Horizontal">
                <wpf:MaterialIcon Kind="TickCircle"/>
                <TextBlock VerticalAlignment="Center" Margin="8 0" Text="Ок"/>            
            </StackPanel>
        </Button>
        <Button HorizontalAlignment="Stretch" Grid.Column="3" Grid.Row="5" Command="{Binding CloseCommand}">
            <StackPanel Orientation="Horizontal" >
                <wpf:MaterialIcon Kind="CancelCircle"/>
                <TextBlock VerticalAlignment="Center" Margin="8 0" Text="Отмена"/>            
            </StackPanel>
        </Button>
        
    </Grid>
</UserControl>